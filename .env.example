# RTMS 환경 변수 — 이 파일을 .env 로 복사 후 값 수정
# cp .env.example .env

# 필수: 세션 보안 키 (임의의 긴 문자열)
SECRET_KEY=your-secret-key-here-change-this

# 데이터베이스 (Render 배포 시 자동 주입됨)
# 로컬 개발: 생략 시 sqlite:///data.db 사용
# DATABASE_URL=postgresql://user:pass@host:5432/rtms

# 서버 포트 (로컬 개발용)
FLASK_PORT=5001
